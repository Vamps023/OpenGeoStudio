Fix ABI mismatch + remove all old TypeScript geometry implementations

CRITICAL FIX: The C++ addon was compiled for Node.js ABI (137) but Electron
uses ABI 146. The addon couldn't load in Electron, causing silent fallback
to TypeScript. Now rebuilt with --target=42.5.1 --dist-url=electronjs.org/headers.

Changes:
- binding.gyp: Rebuilt against Electron 42.5.1 headers (ABI 146)
- package.json: Updated build:road-engine and added rebuild:road-engine
  scripts with correct Electron target
- types.ts: REMOVED all geometry implementations:
  - bezierPoint, sampleRoad (→ C++ sampleCenterline)
  - segmentIntersection, lineIntersection, findCenterlineIntersection
  - closestPointOnRoad, tangentAtSample, normalAtSample, filletArc
  - computeIntersectionPolygon, convexHull
  - generateIntersection + all helper functions
  - computeCircleArc, arcToControlPoints
  KEPT: type definitions, generateId, geoToLocal, localToGeo,
  distanceMeters, detectIntersections (lightweight endpoint check)
- roadEngineClient.ts: Removed TypeScriptRoadEngineFallback class entirely.
  C++ native engine is the ONLY backend. Throws if not available.
  Updated sampleCenterline and generateRoadMesh to take refLat/refLon.
- roadStudioStore.ts: All sampleRoad() calls → roadEngine.sampleCenterline()
  (async). Removed TS generateIntersection fallback. createIntersectionAtClosestPoint
  is now async. detectIntersection fails loudly if C++ engine fails.
- RoadViewport.tsx: Removed sampleRoad, computeIntersectionPolygon, convexHull,
  computeCircleArc imports. Added sample cache (refreshSampleCache) populated
  async by C++ engine, used sync for rendering. computeIntersectionPolygonFromCache
  replaces the old TS function using cached samples.
- RoadElevationEditor.tsx: Removed unused sampleRoad import.

Verified: C++ addon loads in Electron (tested with npx electron -e).
All 11 tests pass with C++ logging output visible.

Generated with Devin (https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
