Fix map not visible: AABB prune + correct zoom calculation

Two bugs fixed:

1. Tiles pruned immediately (AABB fix)
   The distance-based prune check was too aggressive. At zoom 9,
   the tile center was ~39000m from camera but viewRadius+tileHalf
   was only ~38592m — so the tile was pruned even though it
   covered the visible area.

   Fix: Replaced distance-from-center check with proper AABB
   (axis-aligned bounding box) overlap test. A tile is kept if
   its bounding box intersects the visible area rectangle.

2. Wrong zoom level (too low detail)
   The zoom calculation used targetTileMeters = viewSize * 0.5
   which resulted in zoom 9 (one 74km tile for a 600m view).
   This made the map extremely blurry.

   Fix: Changed to targetTileMeters = viewSize, which gives
   zoom 16 (appropriate detail for a 600m view, ~12 tiles).

Debug logging was added to trace the issue, then removed.

Verified working:
  - Zoom 16, 12 tiles requested (4x3 grid)
  - Tiles downloaded (256x256 each, ~20KB)
  - Composite image created and uploaded as single texture
  - Quad drawn successfully

Build: OK | Tests: 473/473 passed | App: runs

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
