Remove all placeholder/synthetic data from export engine

Replaced every placeholder fallback with proper error reporting:

DEM download (downloadDemForTile):
- Empty URL: was writing flat 0.0m heightmap, now emits error with
  specific reason (missing API key, token, or file path)
- Decode failure: was generating synthetic sine-wave terrain, now emits
  error with data type details (PNG/TIFF/AAIGrid), byte count, and source

Local DEM (loadLocalDemForTile):
- Missing file: was writing flat 0.0m heightmap, now emits error
- Load failure: was writing flat heightmap, now emits error with path

Local imagery (loadLocalImageryForTile):
- Missing file: was filling darkGreen image, now emits error
- Load failure: was filling darkGreen, now emits error with path

Imagery download (downloadImageryForTile):
- Empty URL: was filling darkGreen, now emits error with reason
- Network error: was saving darkGreen image, now emits error
- Decode failure: was filling darkGreen, now emits error with byte count

All errors now stop the export with a descriptive message instead of
silently writing fake data. The only intentional skip is
HeightmapFormat::None (user chose "albedo only" — no DEM output needed).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
