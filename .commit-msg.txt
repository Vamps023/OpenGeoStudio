Add SCANeR-style road profile catalog with 20+ presets

Add a comprehensive RoadProfileCatalog to RoadTypes.hpp with 20+
predefined road profiles organized by category:
- City (2x1, 2x2, 2x3, one-way variants)
- Country/Rural (2x1, 2x2, narrow)
- Highway (2x2, 2x3, 2x4)
- Ramp/Interchange (1x1, 1x2)
- Roundabout (1x1, 2x1)
- Parking/Service (1x1 each)
- Asymmetric divided (2x3, 1x2)
- Custom

Each profile defines: lane width, left/right lane counts, offsets,
speed limit, sidewalk, curb, surface/marking textures, and description.

Add a profile selector dropdown (QComboBox) to the Road Studio top
toolbar. Selecting a profile updates:
- Global LM::LaneWidth
- Left/right lane plans via LaneConfigWidget::SetOption
- Lane width spinner value
- Viewport repaint

Extended RoadProfile struct with leftLanes, rightLanes, leftOffsetX2,
rightOffsetX2, speedLimit, and description fields. Updated toJson/fromJson
for full serialization. Added RoadProfileCatalog with all(), get(),
profileNames(), and label() static methods.

Build: SUCCESS
Tests: 504/504 passed, 5388 assertions, 0 failed
