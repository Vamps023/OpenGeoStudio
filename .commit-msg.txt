Phase 3 prep: 3D debug overlays + Road Inspector panel

Track 1 — Debug Overlays (RoadViewport.tsx):
  - 4 new debug layers: laneCenters, laneBoundaryLines, laneIds, meshWireframe
  - Lane centerlines rendered as colored lines (yellow=center, green=right, red=left)
  - Lane boundaries rendered as lines (white=road edge, teal=inner boundaries)
  - Lane IDs rendered as colored spheres at centerline midpoints
  - Mesh wireframe rendered as magenta line overlay on asphalt triangles
  - All overlays use cached buildRoad() data (no extra C++ calls)
  - Toggle via Debug mode in toolbar

Track 2 — Road Inspector Panel (RoadInspector.tsx):
  - New right-dock panel registered as 'road-inspector'
  - Shows road metadata: name, ID, width, lanes, length, control points
  - Shows road profile: type, surface, lane width, sidewalk, curb toggles
  - Shows lane engine data: lane sections, centerlines, boundaries, road marks
  - Shows per-lane details: ID, length, sample count
  - Shows mesh statistics: vertices, triangles, sections per material
  - Shows adapter report: exact/legacy/unsupported segment counts + warnings
  - Shows intersection connections: start/end node IDs
  - Uses existing FormSection, NumberField, TextField, SelectField, BooleanField
  - Live-updates when road selection changes

Track 3 — Phase 3 C++ (in progress):
  - road_graph.hpp being created (RoadGraph, RoadNode, RoadEdge)

Build: clean. Tests: 141/141 pass.
