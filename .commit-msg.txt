Phase 16: Complete feature parity — elevation editor, validation, settings, command palette

Implemented all critical and medium-priority features identified in the
feature gap audit against the reference Electron app.

Road Studio:
- Added RoadElevationEditor: SCANeR-style s vs z elevation profile graph
  with filled area, control point dots, z/s axis labels, grid
  Tools: Flat, Slope Up, Slope Down, Bridge, Roller
  Shows automatically when a road is selected, hidden otherwise
- Added Finish Road button to toolbar (visible when drawing, Enter key)
- Added road length display to inspector (computed in meters)
- Added control point count display to inspector

Train Studio:
- Added XML validation: validateNetwork() checks for:
  - Empty networks, tracks with < 2 points
  - Invalid gauge, invalid coordinates
  - Duplicate consecutive points
  - Unconnected tracks (warnings)
- Added Validate button with Ctrl+Shift+V shortcut
- Added validation status badge (green/yellow/red) in toolbar
- Added Snap toggle button in toolbar

Home:
- Added project pin/unpin toggle buttons on each recent project row
- Pinned projects now sorted to top
- Pin button uses star icon (filled for pinned, outline for unpinned)

Global:
- Added Command Palette (Ctrl+Shift+P): searchable command list with
  file operations, workspace switching, settings, help
- Added Settings Dialog (Ctrl+,): API keys (OpenTopo, Mapbox, MapTiler),
  project defaults (workspace, road width, lane count)
- Added workspace switching shortcuts (Alt+1 through Alt+4)
- Added cursor coordinate display in status bar for all map workspaces
  (shows Lat, Lon, Zoom on mouse move)

Build:
- Added RoadElevationEditor.cpp to CMake for MOC processing
- Added QPainterPath include to elevation editor

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
