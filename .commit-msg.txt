Implement real terrain rendering + fix scene persistence issues

CRITICAL FIX: loadTerrain() was a stub
- Was: just logged "Terrain load requested" and returned
- Now: loads heightmap PNG via QImage, creates heightfield mesh
  using OGRE ManualObject, samples grayscale values into elevation,
  computes normals from neighboring pixels, applies albedo texture
  via HlmsUnlitDatablock, creates Item + SceneNode
- Grid resolution capped at 200x200 to balance detail/performance
- Camera auto-frames terrain after loading

CRITICAL FIX: clearTerrain() was a stub
- Was: empty function
- Now: properly destroys terrain Item and SceneNode

FIX: shutdownOgre() now clears objects
- Was: only cleared roads and terrain
- Now: clears objects, roads, and terrain in proper order

FIX: Scene JSON now uses relative paths
- Was: absolute paths (broke if project moved)
- Now: paths relative to project base path
- onSaveScene converts absolute → relative
- resolveScenePaths helper converts relative → absolute on load

FIX: findHeightmapInProject/findAlbedoInProject
- Now prefers merged heightmap/albedo files over individual tiles

Houston 4km end-to-end test: PASS
- 16 heightmap tiles + 16 albedo tiles downloaded
- Merged into 1024x1024 images
- 5 roads in XODR
- 7 objects in scene JSON (relative paths)
- App launched, terrain rendered, stable 35s (925MB)
- Closed, reopened, scene auto-loaded, stable 40s (1.3GB)
- No errors in OGRE log
- All files intact, all references valid

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
