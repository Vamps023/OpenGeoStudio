Phase 3: Qt application shell with ApplicationContext, ProjectManager, WorkspaceManager, and Home workspace

Core services ported from the Electron/TypeScript reference:

- ApplicationContext: Central service container (replaces AppContext + ServiceRegistry)
- EventBus: Lightweight pub/sub (replaces core/events/event-bus.ts)
- Logger: Scoped logging with levels (replaces core/logger/logger.ts)
- ProjectManager: .ogproj CRUD, folder structure, recent projects, autosave
  (replaces core/project/project-manager.ts)
- WorkspaceManager: 4 workspaces (home, terrain, road-studio, train-studio)
  with activation and switching (replaces core/workspace/workspace-manager.ts)

UI:
- HomeWidget: Start screen with template cards (Terrain, Road Studio),
  recent projects list with search, open project button
  (replaces renderer/panels/RecentProjects/RecentProjects.tsx)
- MainWindow: QStackedWidget for workspace switching, dock widgets,
  menu bar, toolbar, dark theme

The app now starts on the Home workspace. Switching to Terrain or Road
Studio shows the Esri satellite map from Phase 2c. Project creation
creates the full folder structure (Terrain, GIS, Roads, etc.) and saves
a .ogproj file. Recent projects are persisted to app data.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
