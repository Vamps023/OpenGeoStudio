Fix: rebuild native addon against Electron ABI + correct Python path

The road engine addon was built against Node.js ABI 137 but Electron
42.5.1 uses ABI 146, causing the Electron main process to crash on
addon load. Rebuilt with node-gyp --target=42.5.1 against Electron
headers. Also fixed the Python path in build scripts (was pointing to
non-existent python3.13.exe, now uses Python 3.12 installed via winget).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
