Fix terrain zoom, search bar, and panel alignment

Zoom (Issue 1):
- Add wheelEvent to TerrainOverlayWidget — scroll to zoom toward cursor
  (MapLibre/QGIS pattern: zoom + adjust center to keep cursor geo point)
- Add zoom in/out buttons (➕/➖) to toolbar with keyboard shortcuts
- Add "Fit" button to fit map to current selection bounds
- Zoom clamped to 0-20 range

Search bar (Issue 2):
- Connect SearchBar::locationSelected signal to map — selecting a result
  now flies the map to that location with type-specific zoom
- Fix dropdown layout: was in QVBoxLayout which resized the search bar
  when shown. Now uses Qt::ToolTip window flag as floating popup
- Position dropdown below input using mapToGlobal
- Add search icon emoji to placeholder text
- Increase input height to 34px for visibility
- Status bar shows "Flew to lat, lon (zoom N)" on location select

Panel alignment (Issue 3):
- Increase ExportPanel max width from 320 to 400px
- Add minimum width of 340px so form labels don't get truncated
- This prevents combo boxes and form rows from being squished

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
