Fix 2D map rendering and road drawing — tiles load, coords correct

Root causes identified via debug logging:

1. Map texture never loaded:
   - toggleViewMode() called loadMapBackground() but the debug timer
     only called SetViewMode() — confirmed tiles DO load when
     loadMapBackground() is actually called
   - All 9 Esri tiles fetch successfully and composite into 768x768 image
   - drawMapBackground() renders the texture correctly

2. PointerOnGround returned wrong Z coordinate (505 instead of 0):
   - Old code unprojected at NDC z=0 which maps to mid-depth in
     orthographic projection, not to world z=0
   - Fixed: unproject two points at NDC z=-1 and z=1, then
     intersect the ray with the z=0 ground plane
   - Now returns correct world (x, y) coordinates on z=0 plane

Cleanup:
- Removed debug timer from main.cpp
- Removed debug logging from drawMapBackground, PointerOnGround,
  toggleViewMode, loadMapBackground
- Removed unused includes from main.cpp
- Made loadMapBackground() public (used by Map button and toggleViewMode)
- Renamed StyleHttpServer to LmStyleServer (fix persistent linker conflict)

Build: OK | Tests: 473/473 passed | App: 2D mode loads satellite tiles

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
