Phase 2c: MapLibre Native Qt rendering spike - Option A prototype

- Built maplibre-native-qt from source (473 targets, Qt 6.8.0 + OpenGL)
- Installed to D:/git/maplibre-native-qt/install (QMapLibre + QMapLibreWidgets)
- Created MapViewportWidget wrapping QMapLibre::MapWidget
- Esri World Imagery raster tiles (same as reference app's SkiaViewport)
- Centered on Pune, India (lat=18.52, lon=73.85, zoom=15) for parity
- Map click -> status bar coordinate display (proves geo conversion works)
- CMake finds QMapLibre via find_package(QMapLibre COMPONENTS Widgets)
- Post-build copies QMapLibre.dll + QMapLibreWidgets.dll to deploy dir
- App launches and runs with live satellite map background
- HAVE_MAPLIBRE compile-time guard with placeholder fallback

Key finding: MapLibre Native Qt builds and works with Qt 6.8.0 on Windows.
The GLWidget (QRhiWidget subclass) renders Esri raster tiles correctly.
Option A is viable - no need for Option B (QGraphicsView) fallback.

moc issue resolved: raw string literals in class body confuse moc.
Moved kEsriImageryStyle constexpr to .cpp file.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
