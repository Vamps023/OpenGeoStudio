Add straight line tool and move LaneMaker menus to main app bar

Straight Line Tool:
- New "─" button in Road Studio sidebar toolbar
- Click two points to create a straight road — no direction handle,
  no multi-click staging, no confirm step needed
- Uses odr::Line geometry directly (same as LaneMaker's collinear case)
- Auto-completes after the second click
- Added Mode_StraightLine to EditMode enum
- Added forceStraightLine and autoCompleteAfterFirstSegment flags
  to RoadCreationSession
- Replay system updated to handle the new mode

Menu Bar Integration:
- Removed LaneMaker's internal menu bar (File/Edit/Replay/Simulation)
- Added Road Studio menus to the main app's menu bar instead
- Menus appear when Road Studio workspace is activated
- Menus are hidden when switching to other workspaces
- All actions wired to LaneMaker's MainWindow methods:
  - Road File: New, Open, Save, Preferences
  - Edit: Undo, Redo, Verify Now
  - Replay: Save, Debug, Watch
  - Simulation: Toggle simulation, Paused
- Exposed MainWindow methods as public for external menu integration
- Simulation toggle disables Undo/Redo while active (same as before)
- Pause action only enabled when simulation is active

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
