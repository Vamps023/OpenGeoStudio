Phase 1.8.3b: roadToV2() — Bezier/Arc/Spiral exact reconstruction + AdapterReport

Adds exact reconstruction for all four geometry segment types,
with explicit missing-metadata handling (no silent fallback).

Changes:
- ControlPoint: added optional SegmentMetadata for arc/spiral parameters
- AdapterReport: diagnostics struct tracking exact/approx/unsupported counts
- Bezier: exact from handles (P0, P0+handleOut, P1+handleIn, P1)
- Arc: exact from metadata (startPoint, heading, curvature, arcLength)
- Spiral: exact from metadata (startPoint, heading, k0, k1, length)
- Unknown metadata type → warning + LineSegment fallback
- Bezier metadata without handles → warning + LineSegment fallback
- roadToV2(const Road&, AdapterReport&) overload for diagnostics

Tests (18 new, 189 total, 847 assertions):
- Bezier: exact reconstruction, endpoint positions, golden fixtures
  (bezier_arch, mixed_line_bezier — segment types + positions at CPs)
- Arc: exact reconstruction, start position, length, constant curvature
- Spiral: exact reconstruction, length, curvature transition k0→k1
- AdapterReport: all-line counts, mixed counts, unknown type warning,
  bezier-without-handles warning
- Combined: line + bezier + arc + spiral in one road (4 segments)

All 113 vitest tests remain green (no regression).
Legacy Road path is completely untouched.
