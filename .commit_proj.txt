Replace hand-written UTM math with PROJ-backed transforms

GISProcessor::transformPoint() now uses gis::CRSManager and
gis::CoordinateTransform (PROJ) for all CRS pairs except the fast
inline WGS84<->WebMercator path. The hand-written UTM series
expansion formulas (lonLatToUtm/utmToLonLat, ~80 lines) have been
removed in favour of PROJ's authoritative transformations, which
are accurate for all UTM zones and hemispheres.

The CrsSpec interface is preserved as a wrapper so the entire
terrain pipeline (TerrainManager, TileManager, MaskManager,
ValidationManager, GISProcessor) continues to work without API
changes. The test_terrain_pipeline target now links against PROJ
and includes the CRSManager/CoordinateTransform sources.

Verified: 28/28 terrain pipeline tests pass, 7/7 CTest targets pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
