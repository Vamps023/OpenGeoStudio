#pragma once
// Root-level geometry.hpp — redirects to the public engine header
// to avoid duplicate definitions when both root and public headers
// are included in the same translation unit.
#include "road_engine/public/geometry.hpp"
