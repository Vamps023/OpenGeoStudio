Integrate WorldBuilder into production Studio3D

- Add WorldBuilder-driven procedural generation to OgreWidget:
  generateBuildings, generateVegetation (PCG), addLake, addSunLight,
  addSkyLight, buildRoadSpline
- Wire WorldBuilder's sampleHeight to OgreWidget terrain sampling so
  generated actors sit on the terrain
- Add syncBuilderToWorld() that copies builder world into the render
  world and rebuilds renderables
- Keep WorldBuilder in sync when loading a world from file
- Add "World Authoring" panel to Studio3DWidget UI with buttons for
  buildings, vegetation, lake, sun, sky generation
